"use client";
import { StyledTab, StyledTabs } from "@components/Tab/CustomizedTab";
import React, { useEffect, useMemo, useState } from "react";

const Resellers = () => {
    return (
        <div className="px-12">Resellers</div>
    );
};

export default Resellers;
