"use client";
import { StyledTab, StyledTabs } from "@components/Tab/CustomizedTab";
import React, { useEffect, useMemo, useState } from "react";

const ShipCustomers = () => {
    return (
        <div className="px-12">Ship Customer</div>
    );
};

export default ShipCustomers;
