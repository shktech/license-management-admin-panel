"use client";
import { StyledTab, StyledTabs } from "@components/Tab/CustomizedTab";
import React, { useEffect, useMemo, useState } from "react";

const BillCustomers = () => {
    return (
        <div className="px-12">Bill Customer</div>
    );
};

export default BillCustomers;
